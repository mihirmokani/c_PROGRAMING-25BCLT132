#include <stdio.h>
int main() {
    float s1, s2, s3, avg;
    printf("Enter s1, s2, s3: ");
    scanf("%f %f %f", &s1, &s2, &s3);
    avg = (s1 + s2 + s3) / 3;
    printf("Average: %.2f\n", avg);
    return 0;
}
