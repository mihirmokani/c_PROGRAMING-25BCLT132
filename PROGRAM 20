#include <stdio.h>
int main() {
    float h,l,a ;
    printf("Enter height of triangle: ");
    printf("Enter base of triangle: ");
    scanf("%f %f", &h, &l);
    a=(h*l)/2;
    printf("Area of triangle: %f\n", a);
}

